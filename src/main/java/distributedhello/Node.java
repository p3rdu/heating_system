package distributedhello;

import java.util.*;

public class Node {
    String hostname;
    int port;

    public Node(String hostname, int port) {
        this.hostname = hostname;
        this.port = port;
    }
}
